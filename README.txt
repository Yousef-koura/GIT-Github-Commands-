
first choose the path and then open git bash
to start a repository:
git init "project_name"           # it creates a folder with a project name
ls                                             # to list what is in the folder
cd                                           # to move paths
touch filename.type		     # create a file 
git status                                 # to tell you the status of the project and the files not commited
git add .                   
git commit -m"comment"
git log